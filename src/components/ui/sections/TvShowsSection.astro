---
import SectionList from './SectionList.astro';
import type { SectionConfig } from './section-utils';

const config: SectionConfig = {
  key: 'tv-show-relax',
  title: 'Chương Trình TV Giải Trí',
  subtitle: 'TV show giải trí nổi bật',
  typeList: 'tv-shows',
  layout: 'portrait',
  params: {
    page: '1',
    sort_field: 'modified.time',
    sort_type: 'desc',
    limit: '12',
  },
};
---

<SectionList config={config} />
